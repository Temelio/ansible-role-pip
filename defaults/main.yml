---

# Defaults vars file for pip role

# General variables
pip_packages: "{{ _pip_packages | default([]) }}"


# Specific Debian family vars
pip_repository_update_cache: True
pip_repository_cache_valid_time: 3600
